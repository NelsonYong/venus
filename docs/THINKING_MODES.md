# 思维链和任务展示功能使用说明

## 概述

系统现在支持两种思维过程展示模式：
1. **Chain of Thought（思维链）** - 用于展示复杂的推理过程
2. **Task（任务）** - 用于展示具体的操作步骤

这两种模式都是**流式渲染**的，AI 会实时展示每一步的思考过程。

## 如何触发思维链

AI 会根据问题的复杂度**自动**决定是否使用思维链。以下是一些触发示例：

### 会触发 Chain of Thought 的问题类型

**复杂分析问题：**
```
比较 React 19 和 Vue 3 的优缺点
```

**多步骤推理：**
```
如何设计一个可扩展的微服务架构？
```

**需要综合信息：**
```
分析这段代码的性能问题并提供优化方案
```

**比较和评估：**
```
评估这三种数据库方案，帮我选择最适合的
```

### 会触发 Task 的问题类型

**文件操作：**
```
帮我分析这个项目的目录结构
```

**代码处理：**
```
读取 package.json 并列出所有依赖
```

**搜索和查找：**
```
在代码库中找到所有的 API 端点
```

### 不会触发思维链的问题类型

**简单事实问题：**
```
JavaScript 的 map 函数是做什么的？
```

**直接代码生成：**
```
写一个计算斐波那契数列的函数
```

## 效果展示

### Chain of Thought 效果

当 AI 处理复杂问题时，你会看到：

```
🧠 Chain of Thought

▼ Analyzing the problem

  ● Understanding requirements          [完成]
    Breaking down user's question
    
  ⊙ Gathering information              [进行中]
    🔍 Searching documentation
    
  ○ Synthesizing answer                [待处理]
```

每一步都会实时出现，你可以看到 AI 的思考过程。

### Task 效果

当 AI 执行具体任务时，你会看到：

```
🔍 Processing files                    ▼

  │ Reading configuration
  │ 📄 config.json
  │
  │ Analyzing code structure
  │
  │ Generating report
```

## 技术实现

### 后端

AI 使用 `thinkingStep` 工具来发送思维步骤：

```typescript
// Chain of Thought
thinkingStep({
  stepType: "chain-of-thought",
  title: "Analyzing the problem",
  stepId: "step1",
  label: "Understanding requirements",
  description: "Breaking down user's question",
  status: "complete"
})

// Task
thinkingStep({
  stepType: "task",
  title: "Processing files",
  stepId: "task1",
  label: "Reading configuration",
  files: ["config.json"],
  status: "complete"
})
```

### 前端

前端会自动：
1. 聚合相同 title 的思维步骤
2. 按照步骤顺序渲染
3. 实时更新状态（pending → active → complete）

## 自定义提示

如果你想明确要求 AI 展示思考过程，可以在问题中加入：

```
请详细展示你的思考过程，帮我...
```

或者：

```
请一步步分析并解释你的推理...
```

## 注意事项

1. **移动端默认不显示**：为了更好的性能，移动端默认不展示思维链
2. **自动折叠**：Chain of Thought 默认折叠，可点击展开
3. **流式渲染**：每一步都会实时出现，无需等待完整响应

## 优势

- ✅ **透明度**：用户可以看到 AI 如何思考
- ✅ **可信度**：展示推理过程增加信任
- ✅ **教育性**：用户可以学习 AI 的分析方法
- ✅ **调试性**：帮助发现 AI 的推理错误

## 示例对话

**用户**：比较 TypeScript 和 JavaScript 的优缺点

**AI 响应**：
```
🧠 Chain of Thought

▼ Comparing TypeScript and JavaScript

  ● Identifying key differences     [完成]
    Type system, tooling, ecosystem
    
  ● Analyzing advantages            [完成]
    TypeScript: type safety, IDE support
    JavaScript: simplicity, flexibility
    
  ● Evaluating trade-offs          [完成]
    Learning curve vs long-term benefits
```

然后跟随正常的回答...

---

开发者：[@your-name]
更新时间：2025-01-05

